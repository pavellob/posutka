# Realty Calendar Adapter Environment Variables
# Все переменные окружения теперь находятся в корневом .env файле
# См. /env.example в корне проекта

# Переменные, используемые этим адаптером:
# - REALTY_CALENDAR_ADAPTER_PORT (по умолчанию 4201)
# - REALTY_CALENDAR_DEFAULT_ORG_ID (по умолчанию default-org)
# - REALTY_CALENDAR_MOCK_SERVER_PORT (по умолчанию 5101)
# - REALTY_CALENDAR_TARGET_URL (по умолчанию http://localhost:4201/webhooks/realty-calendar)
# - BOOKINGS_GRPC_HOST (по умолчанию localhost)
# - BOOKINGS_GRPC_PORT (по умолчанию 4102)
# - INVENTORY_GRPC_HOST (по умолчанию localhost)
# - INVENTORY_GRPC_PORT (по умолчанию 4101)
# - GRPC_TIMEOUT (по умолчанию 5000)
# - GRPC_RETRY_ATTEMPTS (по умолчанию 3)
# - GRPC_RETRY_DELAY (по умолчанию 1000)
